# Sun-Short-Link (短链接)


## Usage

### Parameter
```
  -c string
        Path to config file
  -i    Generate example config file
```

### Run
```go
go run main.go -i

go run main.go -c config.yml
```